# AGENTS

This file is the **root instruction** for humans and LLMs working in this Dava.Flow Context Warehouse. It gives universal rules and orientation; **detailed process, TDD, review, and completion behaviour live in the Flow Skills installed under `skills/`**.

## Getting Started (Agents)

The human kicks off a session by saying `Let's Flow` (or `/flow` / `$flow` in IDEs that register the command). When that happens:

1. Respond per the `flow.agent` skill: check `.flow/config.toml` for `pending_setup`, then route to the selected phase agent (`signal.agent`, `explore.agent`, or `govern.agent`).
2. Load any skill referenced by the task by reading `skills/<slug>/SKILL.md` **before** acting. Use `dft skills list` to see what is installed; if a required skill is missing, ask the human before installing it with `dft skills add <slug>`.
3. Make the smallest reversible change. Follow TDD for code edits.
4. When closing out, report: files changed, commands/tests run, checks skipped (with reasons), and links to affected code/docs/specs/tasks.

## Installed Skills

Skills are **local to this warehouse**: they live under `skills/<slug>/` and are the only skills an agent may act on. `dft` is responsible for installing, updating, and removing them; agents must not fetch skill content from outside the warehouse.

The table below is generated by `dft init` and refreshed by `dft skills add|remove|update|clean`.

<!-- skills-table-start -->
| Slug | Name | Description | Phase | Kind |
|------|------|-------------|-------|------|
| explore.agent | Explore Agent | 6-step Explore Agent process: Explore Bundle, Discovery, Ideation, Solution Design, Specification & Backlog, and Refinement with Explore Type adaptation and Human-First steering team pattern | explore | agent |
| explore.util.document-ingestion | Document Ingestion | Convert PDF, DOCX, and PPTX files into Markdown for use in context warehouse processes | explore | utility |
| flow.agent | Flow Agent | Cross-cutting entry point for all Dava.Flow work. Presents a phase selection menu and routes to the appropriate phase agent: Signal, Explore, Govern, or Evolve. | flow | agent |
| flow.guide.dft | Dava.Flow Turbine CLI Guide | Progressive-disclosure reference for the dft CLI. Provides an index of all command families with brief descriptions and pointers to --llm docs for deeper detail. Prefer dft over manual file operations whenever a deterministic command exists. | flow | guideline |
| flow.guide.git | Git Conventions Guide | Constrained git instructions for LLM and human use in Context Warehouses. Covers allowed/disallowed commands, branch naming, commit message format, and safe operational patterns. Load this to know what you can and cannot do with git. | flow | guideline |
| flow.proc.setup | Warehouse Setup | Guided first-run setup process for a newly initialised Dava.Flow warehouse. Walks the user through project identity, skills verification, integration configuration, and workspace finalisation. Triggered automatically by flow.agent when pending_setup = true. | flow | process |
| flow.util.output-decoration | Output Decoration | Canonical output decoration conventions for all Dava.Flow skills and agents — heading hierarchy, emoji palette, banner formats, progress indicators, and handoff lines. | flow | utility |
| flow.util.task-definition | Task Definition | Deterministic task scaffolding for dft task create — provides the default basic.toml template manifest (ADR 260418) and task.md LLM-process template for single task creation. | flow | utility |
| govern.agent | Govern Agent | Phase-level orchestrator for Govern work. Determines available tasks via `dft task next`, loads project context from .flow/govern.toml, delegates to the appropriate govern skill, and validates outputs via a clean-context subagent before acceptance. | govern | agent |
| govern.proc.task-check | Task Check | Review tasks from the planning process for completeness, quality, and standards alignment before implementation begins | govern | process |
| govern.proc.task-implementation | Task Implementation | Implement features and changes with Human + LLM pair programming, following test-driven development in small, reversible increments | govern | process |
| govern.proc.task-planning | Task Planning | Create technical implementation plans from task definitions through detailed analysis and decision-making | govern | process |
| govern.util.task-sizing | Task Sizing | Repeatable task complexity estimation using multi-axis scoring framework for implementation, analysis, or process tasks | govern | utility |
| signal.agent | Signal Agent | Use this skill when a user reports a new observation, problem, opportunity, or risk that needs capturing -- transforms raw observations into validated Signal Seeds with 7 completion criteria and explicit routing decisions, the entry point for all new observations entering DavaFlow | signal | agent |
<!-- skills-table-end -->

If the table is empty, skills were not installed (e.g. offline `dft init`). Run `dft skills list` to confirm the current state and `dft skills add <slug>` once online.

### Skill Loading

- Load a skill by reading `skills/<slug>/SKILL.md` before executing any step it governs.
- Only load skills whose description matches the task at hand; do not speculatively load unrelated skills.
- Treat skill content as **authoritative** over this file for the domain it covers.
- If a task references a skill that is not installed, stop and ask the human — do not invent skill content.

## Step Execution

When executing any skill's step files, follow this rule at all times:

**ONE STEP AT A TIME.** Read step → Execute step → Complete step → Next step.

Forbidden:

- Reading ahead into later step files.
- Batching or merging multiple steps into a single action.
- Skipping step files.
- Auto-advancing past a step that requires human confirmation.

This applies to every skill's `steps/` directory. Skills may add further constraints (e.g. mandatory pauses, validation gates) but must never relax this rule.

## Project Context

### Purpose

<!-- PROJECT-SPECIFIC: Replace the TODO line with this warehouse's actual purpose. -->
<!-- Example: "Dava.Flow Turbine — the deterministic CLI (`dft`) for Dava.Flow Context Warehouses." -->
sdeep_warehouse — A personal Dava.Flow Context Warehouse for coordinating tasks, specifications, and context for sdeep.

### Stack & Entry Points

<!-- PROJECT-SPECIFIC: Replace the TODO line with the primary language(s), frameworks, and canonical commands. -->
<!-- Example: "Go 1.24, Cobra CLI. Run `task test` / `task build`; validate config with `dft config check`." -->
TBD — update once the project technology stack and canonical commands are decided.

### Operating Principles

- Clarity over cleverness.
- Small, reversible steps.
- TDD required for code edits.
- Explicit versions everywhere.
- Link everything (code ↔ docs ↔ specs ↔ tasks).

## Warehouse Structure

This warehouse follows the Dava.Flow phase-aligned structure:

```
.flow/                          # Turbine configuration
skills/                         # Installed Flow Skills
signal/                         # Signal phase — problem qualification
  signals/                      # Signal definitions
explore/                        # Explore phase — discovery and specification
  epics/                        # Epic definitions
govern/                         # Govern phase — planning and implementation
  releases/                     # Release documentation
evolve/                         # Evolve phase — production optimization
work/                           # Active task workflow
  01-pending-planning/          # Tasks awaiting planning
  02-planning/                  # Tasks being planned
  03-pending-implementation/    # Tasks awaiting implementation
  04-implementing/              # Tasks being implemented
  05-pending-completion/        # Implemented tasks pending final completion
  06-completed/                 # Completed tasks
```

Task IDs are allocated sequentially by scanning every stage directory under `work/`,
so a task keeps its ID as it moves between stages. The ID prefix and zero-padded
width are defined by `task_id_prefix` / `task_id_format` in `.flow/config.toml`
(e.g. `DFT-0000` → `DFT-0001`, `DFT-0002`, ...).

## Tooling

### Turbine (dft)

Turbine is the deterministic CLI tool for this warehouse. Use `dft <command>` for:
- Warehouse scaffolding (`dft init`)
- Skill management (`dft skills add|remove|list|update|clean|search|info|create`)
- Task workflow management (`dft task create|next|claim|map`)
- Configuration checks (`dft config check`)
- Activity bindings (`dft activity start|sync|list|status|unbind`)
- Reporting (`dft reports numbers`) and sync profiles (`dft sync`)
- Telemetry controls (`dft telemetry status|flush|clear|disable`)

### Taskfile Integration

If a `Taskfile.yml` is present, use `task <command>` for common operations (test, build, lint, format). Exact canonical commands for this warehouse belong in the **Stack & Entry Points** section above.

## Boundaries

Never, without explicit instruction from the human:

- Rename, renumber, or recycle existing task IDs.
- Change `task_id_prefix` or `task_id_format` in `.flow/config.toml`.
- Move a task between `work/` stages by hand — use `dft task` commands.
- Install, update, or remove skills outside `dft skills …`, or act on skills that are not present under `skills/`.
- Delete phase artefacts under `signal/`, `explore/`, `govern/`, or `evolve/`.
- Commit secrets, tokens, credentials, or absolute local paths.

Ask first before:

- Modifying `.flow/config.toml` or the scaffold structure.
- Adding new runtime dependencies.
- Introducing a new workflow stage, skill, or phase agent.

## Commits & Branches

This project follows [Conventional Commits](https://www.conventionalcommits.org/) and [Conventional Branches](https://conventional-branch.github.io/) standards.

### Commit Format

```
<type>(scope): <description>
```

### Branch Format

```
<type>/scope-slug
```

### Change Types

| Type | Activity | Example |
|------|----------|---------|
| `plan` | Planning session | `plan(DFT-0098): Feature name` |
| `impl` | Implementation | `impl(DFT-0098): Feature name` |
| `feat` | User-visible functionality | `feat(DFT-0098): Add CSV export` |
| `fix` | Bug fix | `fix(DFT-0098): Handle empty input` |
| `refactor` | Internal restructuring | `refactor(DFT-0098): Extract service` |
| `test` | Tests only | `test(DFT-0098): Add regression cases` |
| `docs` | Documentation | `docs(DFT-0042): Update guidelines` |
| `spec` | Specification | `spec(DFT-0050): Spec name` |
| `chore` | Tooling / housekeeping | `chore: Bump dependencies` |

## Documentation Standards

- Keep docs index updated.
- Update component READMEs when behavior changes.
- Prefer link-rich docs for LLM traversal.
- Record significant decisions alongside the relevant phase artifacts (e.g. under `explore/` or `govern/`).
- When closing a task, report: files changed, commands/tests run, what was skipped and why, and links to affected code/docs/specs/tasks.
